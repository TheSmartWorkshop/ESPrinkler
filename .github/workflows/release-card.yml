name: Release card

on:
  push:
    tags:
      - "card-v*"
  # Lets a release be cut from the Actions tab (or API) without a local
  # tag push. softprops/action-gh-release creates the tag from `version`.
  workflow_dispatch:
    inputs:
      version:
        description: "Release tag to create (e.g. card-v0.3.0)"
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: card/package-lock.json

      - name: Build card
        working-directory: card
        run: |
          npm ci
          npm run lint
          npm run build

      # HACS pulls the bundle directly from the release assets by basename
      # (see hacs.json `filename`). Uploading the file at the root of the
      # release (no `dist/` prefix) is what HACS expects.
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version || github.ref_name }}
          name: ${{ inputs.version || github.ref_name }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: card/dist/esprinkler-card.js
